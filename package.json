{
  "name": "porttools",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:hub": "npm run build --workspace=@porttools/hub",
    "dev:hub": "npm run dev --workspace=@porttools/hub"
  }
}
